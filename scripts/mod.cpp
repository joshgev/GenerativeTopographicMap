#include <iostream>

using namespace std;

int main(){

	for(int j = 0; j < L ; j ++){
					int num = (int) Math.pow(M_per_latent_dimension, j	);
					currentIndices[j] = (i / num) % ( (int) Math.pow(M_per_latent_dimension, j + 1));
			}

}
